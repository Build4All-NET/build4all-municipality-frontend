class Department {
  final int id;
  final String name;
  final String description;
  final bool isFixed;

  Department({
    required this.id,
    required this.name,
    required this.description,
    required this.isFixed,
  });
}